import { useState } from 'react'
import { api } from '../services/apiClient.js'
export default function LoginPage({ onLogin }) {
  const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false)
  const submit = async (event) => { event.preventDefault(); setLoading(true); setError(''); try { onLogin(await api.login(email.trim(), password)) } catch (err) { setError(err.message) } finally { setLoading(false) } }
  return <main className="login-page"><section className="login-visual"><div className="login-brand"><span className="brand-mark large">SZ</span><span>SAFEZONE</span></div><div><p className="eyebrow light">INTELLIGENT PUBLIC SAFETY</p><h1>Operational clarity.<br />Safer communities.</h1><p>AI-assisted patrol positioning with explainable risk and human approval at every critical decision.</p></div><small>Authorized personnel only · All activity is auditable</small></section><section className="login-panel"><form onSubmit={submit}><p className="eyebrow">ADMIN PORTAL</p><h2>Welcome back</h2><p className="muted">Sign in with your SafeZone administrator account.</p>{error && <div className="form-error">{error}</div>}<label>Email address<input type="email" value={email} onChange={(e) => setEmail(e.target.value)} autoComplete="username" required placeholder="admin@safezone.local" /></label><label>Password<input type="password" value={password} onChange={(e) => setPassword(e.target.value)} autoComplete="current-password" required /></label><button className="primary-button wide" disabled={loading}>{loading ? 'Signing in…' : 'Sign in securely'}</button><p className="login-help">Privileged accounts cannot be registered publicly. Contact the system administrator for access.</p></form></section></main>
}
